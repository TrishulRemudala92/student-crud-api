class DatabaseConnection {
// is Singleton
//purpose of this class is to connect to database
to connect to database, it takes 5 seconds of time.
}

class StudentRepository { 
public Student addStudent(Student s) {
DatabaseConnection connection= DatabaseConnection.getInstance();
connection.getTable("student").addNew(student);
}

}